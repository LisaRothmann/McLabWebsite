/* === MAIN COLOR THEME OVERRIDE === */

/* Top navbar and logo bar */
.skin-blue .main-header .navbar,
.skin-blue .main-header .logo {
  background-color: #017d41 !important;
  color: white !important;
}

/* Sidebar background */
.skin-blue .main-sidebar {
  background-color: #014b28 !important;
}

/* Sidebar text and icons */
.skin-blue .sidebar a {
  color: white !important;
}

/* Active and hover sidebar tab */
.skin-blue .sidebar-menu > li.active > a,
.skin-blue .sidebar-menu > li:hover > a {
  background-color: #029c50 !important;
  color: white !important;
}

/* Content background and font */
.content-wrapper, .right-side {
  background-color: #f5f5f5;
}

body {
  font-family: 'Segoe UI', sans-serif;
  font-size: 15px;
}

/* Headings */
h2, h3 {
  color: #017d41;
}

/* Uploaded photo preview styling */
img {
  border-radius: 8px;
  margin: 10px 0;
}

/* Make header stretch across the full width */
.full-width-header {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: flex-start;
}

/* Expand the logo section to span entire top */
.main-header .logo {
  width: 100% !important;
  text-align: left;
  padding-left: 15px;
}

/* Remove padding between sidebar and header */
.main-header .navbar {
  margin-left: 0 !important;
}
